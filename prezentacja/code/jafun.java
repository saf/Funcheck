immutable class FuncInteger {
  rep Integer value;
  
  lstate FuncInteger(Integer v) {
    this.value = v;
  }
    
  func Integer cubed() {
    let Integer square = this.value * this.value
    in
      sq * this.value
  }
}